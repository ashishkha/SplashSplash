<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.splashsplash">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StudentRegisterationInfoActivity"></activity>
        <activity android:name=".QueryActivity" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".infoActivity" />
        <activity android:name=".cultActivity" />
        <activity android:name=".useloggedActivity" />
        <activity android:name=".LoggedActivity" />
        <activity android:name=".LogActivity" />
        <activity android:name=".admin2Activity" />
        <activity android:name=".adminActivity" />
        <activity android:name=".Login2Activity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="project.async.com.event.FirebaseToken"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>